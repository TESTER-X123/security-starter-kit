# CLAUDE.md

## 프로젝트 개요
바닐라 HTML/CSS/JS로 만든 보안 진단용 스타터킷입니다. 새 진단 도구나 리포트
페이지를 시작할 때 이 프로젝트의 컴포넌트(버튼/카드/배지/상태 표시)를 복사해서
씁니다. 빌드 도구 없이 `index.html`을 바로 열어서 확인 가능합니다.

## 개발 환경
- OS: Windows
- 직무: 취약점 진단 (정보보안)
- 주 기술 스택: HTML, CSS, JavaScript (바닐라, 프레임워크 없이 작업하는 경우가 많음)

## 언어 및 커뮤니케이션 규칙
- 기본 응답 언어: 한국어
- 코드 주석: 한국어로 작성
- 커밋 메시지: 한국어로 작성
- 문서화: 한국어로 작성 (IMPORTANT)
- 변수명/함수명: 영어 (코드 표준 준수, camelCase)
- 어려운 용어가 나오면 괄호로 짧게 풀어서 설명

## 코딩 스타일
- 들여쓰기: 2칸 스페이스
- 네이밍: camelCase (변수/함수), PascalCase (클래스)
- 매직 넘버 대신 상수로 정의
- 에러/예외 처리를 생략하지 말 것 (특히 파일 접근, 네트워크 요청)

## 보안 작업 관련 규칙 (IMPORTANT)
- .env, secrets/, *.pem, *.key 같은 민감 파일은 먼저 물어보지 않고 절대 읽거나 출력하지 않기
- 진단 스크립트나 PoC 코드를 작성할 때는 대상 시스템에 실제 영향을 주는 동작(삭제, 대량 요청 등) 전에 반드시 확인받기
- 보안 취약점 관련 코드(인증, 입력 검증, 권한 체크)를 만질 땐 어떤 위협을 막기 위한 건지 함께 설명
- 확실하지 않은 보안 판단은 "취약하다/안전하다"라고 단정하지 말고, 근거와 함께 제시
- 진단 결과/리포트 작성 시 발견된 사항 → 위험도 → 권고 조치 순서로 정리

## 추가 선호사항
- 코드 변경 시 무엇을, 왜 바꿨는지 간단히 먼저 설명
- 파일을 새로 만들 때는 먼저 간단한 계획을 말해주고 진행
- 가능하면 외부 라이브러리 의존 없이 순수 HTML/CSS/JS로 구현 (요청 시 Tailwind CDN은 허용)
